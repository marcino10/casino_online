<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/Sign_In.css">
    <title>Document</title>
</head>
<body>
    <form class='register_box' action='/auth/register' method='POST'>
        <h3>Register</h3>
        <input class='Username' type="text" placeholder="Username" required name="nick">
        <input class='email' type="text" placeholder="Email" required name="email">
        <input class='password' type="password" placeholder="Password" required name="password">
        <button class='submit_button' type='submit'>Sign in</button>
    </form>
    <p class="server-msg">
        <% if (error_msg) { %>
            <%= error_msg %>
        <% } else if (success_msg) { %>
            <%= success_msg %>
        <% } %>
    </p>
</body>
</html>